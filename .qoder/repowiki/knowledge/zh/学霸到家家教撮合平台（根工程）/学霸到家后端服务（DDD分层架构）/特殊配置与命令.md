根 pom.xml 聚合四个子模块，需从 wzoto-backend 目录执行 `mvn clean package` 构建全部模块；应用入口在 wzoto-interfaces 模块，可通过 `mvn spring-boot:run -pl wzoto-interfaces` 直接启动；数据库初始化需先执行 sql/init_all_learning_tables.sql，再按需运行 seed_data_*.sql 填充测试数据。